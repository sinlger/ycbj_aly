<section>
  <div class="px-8 pt-20 mx-auto md:px-12 lg:px-24 max-w-7xl">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-12">
      <div class="text-center lg:text-left">
        <p
          class="text-xl leading-tight tracking-tight sm:text-2xl md:text-3xl capitalize lg:text-4xl font-medium text-base-900 lg:text-balance">
          常见问题
        </p>
        <p class="text-base leading-normal mt-4 text-base-500 font-medium">
          关于我们平台的常见问题解答
        </p>
      </div>
      <div
        x-data="{
  activeAccordion: '',
  setActiveAccordion(id) {
  this.activeAccordion = (this.activeAccordion == id) ? '' : id
  }
  }"
        class="relative w-full mx-auto font-normal lg:col-span-2">
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-black">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>端到端加密是如何工作的？</span>
            <svg
              class="size-5 duration-300 ease-out text-accent-500 -rotate-[45deg]"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div x-show="activeAccordion==id" x-collapse="">
            <div class="p-4 pt-2 text-zinc-500">
              端到端加密确保只有发送者和接收者可以访问消息内容。信息在发送者的设备上加密，只能由预期的接收者解密，防止任何中间人（包括平台提供商）访问数据。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>
              如果我忘记了加密密钥，还能访问我的加密数据吗？
            </span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              不能。忘记或丢失加密密钥意味着无法访问您的加密数据。这是一项至关重要的安全措施，确保即使有人未经授权访问您的帐户，他们也无法解密和查看您的消息或文件。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>
              阅后即焚消息功能有什么限制吗？
            </span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              阅后即焚消息旨在在特定时间段后或被接收者阅读后自动删除。但是，请注意，该功能仅从参与对话的设备中删除消息。接收者可能会截屏或使用其他方式捕获内容，这是平台无法控制的。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>
              如果我启用了双重认证但丢失了设备怎么办？
            </span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              丢失设备不会自动危及您的帐户安全。建议您尽快联系我们的支持团队，禁用丢失设备上的双重认证，并在新设备上重新设置。此过程有助于保护您的帐户免受未经授权的访问，同时确保您可以重新获得访问权限。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>
              我的个人信息会被共享或出售给第三方吗？
            </span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              不会，我们严格遵守以隐私为中心的原则。您的个人信息绝不会被共享、出售或货币化。我们优先考虑您的隐私，并确保您的数据保密且只有您自己可以访问。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>我可以在多台设备上使用此平台吗？</span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              是的，您可以在多台设备上访问我们的平台。您的加密数据将在所有设备上保持同步，让您可以随时随地无缝通信并访问您的文件和消息。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>
              文件和文档有存储限制吗？
            </span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              我们的平台为您的文件和文档提供充足的存储空间。但是，根据您选择的定价计划，可能会有存储限制。我们提供灵活的计划以满足您的需求，如果您需要额外的存储容量，还可以选择升级。
            </div>
          </div>
        </div>
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-black': activeAccordion==id, 'text-zinc-600 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-600 hover:text-accent-500">
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none">
            <span>可以从平台导出我的数据吗？</span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6">
              </path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;">
            <div class="p-4 pt-2 text-zinc-500">
              是的，您可以从平台导出您的数据。我们提供导出功能，允许您下载消息、文件和文档，确保您有用于备份或迁移目的的本地副本。这为您提供了对数据的更大控制权和灵活性。
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>